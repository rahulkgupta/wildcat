overwrite: true
schema: https://tilden-io-dev.herokuapp.com/v1/graphql
documents: ['./src/graphql/**/*.graphql']
generates:
  src/graphql/schema.ts:
    plugins:
      - "typescript"
      - 'typescript-operations'
      - "typescript-graphql-request"
    config:
      useIndexSignature: true

